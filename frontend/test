function predictSales() {
  const feature1 = document.getElementById("feature1").value;
  const feature2 = document.getElementById("feature2").value;

  fetch("http://localhost:5000/predict", {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ feature1: feature1, feature2: feature2 })
  })
  .then(response => response.json())
  .then(data => {
    document.getElementById("result").innerText = "Prediction: " + data.prediction;
  })
  .catch(error => console.error("Error:", error));
}

function predictSalesperson() {
  fetch("http://localhost:5000/predict_salesperson", {
    method: "POST",
    headers: { "Content-Type": "application/json" }
  })
  .then(response => response.json())
  .then(data => {
    document.getElementById("salesperson-result").innerText = JSON.stringify(data);
  })
  .catch(error => console.error("Error:", error));
}

function predictRegional() {
  fetch("http://localhost:5000/predict_regional", {
    method: "POST",
    headers: { "Content-Type": "application/json" }
  })
  .then(response => response.json())
  .then(data => {
    document.getElementById("regional-result").innerText = JSON.stringify(data);
  })
  .catch(error => console.error("Error:", error));
}
